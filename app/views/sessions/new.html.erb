<% provide(:title, "Log In")%>
<h1>Log In</h1>
<div>
<%-"Since Sessions don't have a model, we need to give more info to the form_for helper"-%>
<%=form_for(:session, url:login_path) do |f| %>

<%= f.label :email%>
<%=f.email_field :email%>

<%= f.label :password %>
<%= f.password_field :password%>

<%= f.submit "Log In" %>
<% end %>

<p> <%= link_to "Don't have an account?",register_path %> </p>
</div>
